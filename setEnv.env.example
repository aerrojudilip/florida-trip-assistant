OPENAI_API_KEY=your-openai-api-key-here
GOOGLE_API_KEY=your-google-gemini-api-key-here

# Optional defaults for the app's initial configuration
DEFAULT_LLM_PROVIDER=openai
DEFAULT_EMBEDDING_PROVIDER=openai

# Optional: route YouTube transcript requests through a proxy. Needed on most free
# cloud hosts because YouTube frequently blocks datacenter IPs (e.g. a Webshare
# residential proxy URL like http://user:pass@p.webshare.io:80).
# YT_PROXY_URL=
# Or set independently:
# YT_PROXY_HTTP=
# YT_PROXY_HTTPS=

# Required to unlock Home, Add Sources, and View Sources (Ask Questions stays public).
APP_PASSWORD=change-me-before-deploying
